{
  "scripts": {
    "test": "npx jest -maxWorkers=50%",
    "coverage": "jest --coverage",
    "test:watch": "jest --coverage --watch --logHeapUsage --maxWorkers=50%",
    "lint": "eslint --cache src/"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "jest-mock-extended": "^3.0.5",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  }
}
